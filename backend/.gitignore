# --- Dependencies ---
node_modules/
jspm_packages/
typings/

# --- Environment & Secrets ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem

# --- Build Outputs ---
dist/
dist-ssr/
build/
*.bak
*.gho

# --- Database/Storage (Crucial for your CSV setup) ---
# Ignore the actual data but keep the folder structure if desired
backend/database/*.csv
# If you want to keep the directory but ignore files, 
# uncomment the line above and add a .gitkeep file in the folder.

# --- Debug/Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log
.pnpm-debug.log*

# --- IDE & OS Files ---
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db

# --- Docker ---
.dockerignore
# Optional: ignore local docker data if you use named volumes
docker-data/

# --- Vite/Testing ---
.vite/
coverage/
.nyc_output